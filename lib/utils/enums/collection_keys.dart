enum CollectionKeys {
  operations('operations'),
  person('person'),
  appointments('appointments'),
  users('users'),
  galery('galery');

  final String value;
  const CollectionKeys(this.value);
}
